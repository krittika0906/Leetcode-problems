#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>



int main() {
    
    int n,i,ele;
    int f=0;
    scanf("%d",&n);
    int arr[n];
    for(i=0;i<n;i++){
        scanf("%d",&arr[i]);
    }
    scanf("%d",&ele);
    for(i=0;i<n;i++){
        if(arr[i]==ele){
            f=1;
            break;
        }
    }
    if(f==1){
        printf("Found");
        
    }
    else{
        printf("Not Found");
    }

      
    return 0;
}
